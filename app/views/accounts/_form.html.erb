<%= simple_form_for account do |f| %>
  <%= f.input :user_id, as: :select, collection: User.all, label_method: ->(u) { u.email }, value_method: ->(u) { u.id } %>
  <%= f.input :amount %>

  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
