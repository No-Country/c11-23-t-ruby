<!-- Updates account amount -->
<%= turbo_stream.update "amount", partial: "accounts/amount", locals: { account: @account } %>

<!-- append transaction to transactions list -->
<%= turbo_stream.prepend "transactions", @transaction %>

<!-- Shows flash messages -->
<%= turbo_stream.prepend "flash", partial: "layouts/flash" %>
